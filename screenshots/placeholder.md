Drop your screenshot files into this folder with these exact names:

  wpf-main.png    — main video routing page (zones, sources, destinations)
  wpf-themes.png  — theme switcher or side-by-side theme comparison
  wpf-audio.png   — audio zones page
  web-panel.png   — browser/mobile web companion

Then commit and push — the README will display them automatically.
